# Task Governance Rules (v0.1)

- Every user-facing change should have a Task Spec.
- Task Spec must include:
  - release_id
  - at least one PRD reference (FR/AC) when applicable
  - in_scope / out_of_scope
  - definition of done
- Keep tasks small (bounded context). Prefer splitting.
